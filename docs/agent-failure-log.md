# 에이전트 실패 로그

에이전트(또는 에이전트가 만든 장치)가 **틀렸거나 틀릴 뻔한 일**을 한 줄씩 적는다.
목적은 반성문이 아니라 **어디서 막을지 정하는 것**이고, 지표는 **같은 실수가 다시 나오는가**다.

## 적는 법

- **언제:** 사용자가 지적했을 때, 리뷰·테스트·점검이 에이전트의 판단을 뒤집었을 때, 사고 직전에 멈췄을 때.
- **원인은 환경 쪽으로 쓴다.** "모델이 부주의했다"가 아니라 무엇이 없어서 틀릴 수 있었는지.
- **막는 곳**은 넷 중 하나다. 뒤로 갈수록 강하다.
  `그냥 둠`(한 번이고 싸게 잡혔다) · `문서`(`AGENTS.md`·README) · `스킬` · `장치`(스크립트·테스트·CI)
- **재발하면** 새 줄을 쓰지 말고 그 줄의 "재발"에 날짜를 더하고, 막는 곳을 한 단계 올린다.
  `그냥 둠`이 재발하면 문서로, 문서가 재발하면 장치로.
- **적지 않는 것:** 해소 전 보안 노출, 비밀값, 개인정보. 이 파일은 공개 저장소에 있다.
  그런 건 해소 뒤에 적거나 git 밖 `~/study/docs/codex-requests/`에 둔다.
- 범위는 이 저장소 작업이다. 옆 저장소의 실수는 그 저장소에서 적는다.

## 로그

| 발견일 | 무엇이 틀렸나 | 환경 쪽 원인 | 막는 곳 | 재발 |
|---|---|---|---|---|
| 09-10 | 브리핑 평가가 **생성에 실패한 케이스를 "환각 없음"으로 집계**했다 | 러너가 실패를 0점 결과로 채우고 리포트는 실패 메모를 읽지 않았다 | 장치 — 리포트 헤더에 생성·판정 실패 카운터(`2d5d677`) | |
| 09-10 | 평가 리포트가 "모델은 값을 지어내지 않는다"고 **근거를 넘어 결론**냈다 | 채점기가 행 오귀속을 못 보는데 그 한계가 결론 옆에 없었다. 1회 관측에 원인을 붙였다 | 그냥 둠 — 최종 브랜치 리뷰가 잡아 정정(`15d2e37`) | |
| 09-11 | 터널 ingress를 **`:8081`(개발 DB + AI 키)로 잡아** 공개할 뻔했다 | 응답·로그인 화면이 정상이라 어느 스택인지 안 보였다(Cloudflare가 `Server` 헤더를 정규화) | 문서 — 스택 표(#61) → 장치 — 터널 대상 점검(#63) | |
| 09-13 | 병렬 세션이 **이미 해소된 상태**(자동 기동 결손, Funnel 켜짐)를 원장·메모리에 적었다 | 기록 전에 확인하는 규칙이 없었고 세션끼리 서로의 최신 상태를 못 봤다 | 문서 — "확인한 명령과 날짜를 같이"(#61, `~/study/CLAUDE.md`) | |
| 09-13 | 새 세션이 **앞 작업을 전혀 몰랐다** | 앞 세션이 `~/study`에서 열려 메모리·세션 기록이 갈라졌다 | 문서 — 세션을 여는 위치(#61), `~/study/CLAUDE.md` 지도 | |
| 09-13 | 시드 스크립트를 `:8090` 직결로 돌려 **발주 폼 리다이렉트가 `:80`으로** 갔다 | nginx가 `Host`에서 포트를 뗀다 — 돌려 봐야 알았다 | 문서 — `AGENTS.md`·README "공개 주소로 돌린다"(#61) → 장치 — 폼 시드 2개가 `:8090` 주소를 거부 | |
| 09-13 | 가이드 초안에 "상위 폴더에서 열면 이 파일이 로드되지 않는다"고 **도구 동작을 확인 없이 단정**했다 | 공식 문서를 보기 전에 썼다 | 그냥 둠 — 같은 날 문서 확인 후 정정(`ce2d695`) | |
| 09-14 | 로컬 `service-refs.json`만 보고 **"system-tests 기준이 옛 OMS"라고 오판**했다 | 로컬 체크아웃이 원격보다 뒤처져 있었다 | 문서 — "옆 저장소는 fetch 후 원격으로"(#64) | |
| 09-14 | **master 코드 변경이 공개 스택에 반영됐는지 보는 절차가 없어** 공개 스택이 9/10 이미지인 채였다 | 구현 병합·기준 갱신·검증·배포를 나눠 보는 기준이 WMS에 없었다 | 문서 — 완료 기준(#64) + 장치 — 이미지 최신성 점검(#63) | |
| 09-14 | 배포 확인에서 **demo 로그인 403을 장애로 볼 뻔**했다 | 로그인 페이지에 `_csrf` input이 둘이라 확인 명령이 토큰을 이어 붙였다 | 장치 — 점검 스크립트가 첫 토큰만 쓴다(#63) | |
| 09-14 | 점검 스크립트의 `"$TUNNEL_CONFIG에"`가 **한글까지 변수명으로 읽혀** SKIP 경로에서 죽었다 | bash가 변수명 뒤 한글 바이트를 이름에 붙였다. 정상 경로에선 안 탄다 | 그냥 둠 — 병합 전 음성 테스트(입력을 틀어 FAIL 확인)가 잡았다 | |
| 09-14 | 가이드 줄 수를 PR 본문 숫자(58)대로 **확인 없이 사용자에게 말했다**(실제 60) | 요약에 있던 수치를 원본에서 다시 세지 않고 옮겼다 | ~~그냥 둠~~ → 문서 — "수치는 원본에서 다시 센다"(`AGENTS.md`) | 09-14 — 이 로그를 올린 PR #65 본문의 막는 곳 집계를 줄과 대조하지 않고 적었다 |
| 09-15 | 공개 스택 점검을 Docker API에 접근할 수 없는 환경에서 실행해 **컨테이너 3대와 공개 주소를 장애로 오인**했다 | 실행 환경의 Docker 소켓·외부 네트워크 접근 여부를 점검 시작 전에 확인하지 않았다 | 장치 — 점검 스크립트가 Docker API 사전 검사를 한다 | |
